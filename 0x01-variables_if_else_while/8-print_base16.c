#include <stdio.h>
/**
 * main - Entry Point
 *
 * Return: 0 (win)
 */

int main(void)
{
	char n;

	int f;

	n = 'a';
	f = 0;
	while
		(f < 10) {
			putchar(f + '0');
			f++;
		}
	while
		(n <= 'f') {
			putchar(n);
			n++;
		}
	putchar('\n');
	return (0);
}
