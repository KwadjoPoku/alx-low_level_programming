#include "main.h"

/**
 * mul - Multiplies two integers
 * @c: numer 1 to multiply
 * @d: number 2 to multiply
 * Return: The result of the multiplication between c and d param
 */

int mul(int c, int d)
{
	return (c * d);
}
